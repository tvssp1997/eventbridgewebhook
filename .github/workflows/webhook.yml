name: Webhook Triggered Workflow

on:
  repository_dispatch:
    types: [webhook-event]

jobs:
  print-message:
    runs-on: ubuntu-latest
    steps:
      - name: Print Message
        run: echo "Workflow triggered by webhook event"
